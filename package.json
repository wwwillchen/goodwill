{
  "name": "goodwill",
  "version": "0.1.0",
  "description": "Experimental CSS framework",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "start": "gulp sass:watch",
    "sass": "gulp sass",
    "push": "git branch -D gh-pages; git checkout -b gh-pages; git push origin gh-pages; git checkout master",
    "build": "npm run sass; npm run push",
    "test": "protractor test/config/protractor.js"
  },
  "author": "Will Chen",
  "license": "MIT",
  "devDependencies": {
    "eyes.protractor": "0.0.50",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.0",
    "gulp-sass": "^2.2.0",
    "protractor": "3.0.0"
  }
}
